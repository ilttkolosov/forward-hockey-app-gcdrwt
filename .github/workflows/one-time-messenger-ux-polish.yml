name: One-time messenger UX polish

on:
  push:
    branches:
      - fix/minor-fixes-batch
    paths:
      - .github/workflows/one-time-messenger-ux-polish.yml
      - scripts/one-time-messenger-ux-polish.mjs

permissions:
  contents: write

jobs:
  patch-and-validate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: fix/minor-fixes-batch
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Apply messenger UX patch
        run: node scripts/one-time-messenger-ux-polish.mjs

      - name: Format touched files
        run: >-
          npx prettier --write
          components/PersistentBottomNavigation.tsx
          features/messenger/MessageReceiptsModal.tsx
          app/messenger/group/[id].tsx
          app/messenger/room/[id].tsx

      - name: Install dependencies
        run: npm ci

      - name: Bundle Android JavaScript
        env:
          CI: '1'
          EXPO_NO_TELEMETRY: '1'
        run: npx expo export --platform android --output-dir /tmp/forward-hockey-export

      - name: Typecheck and lint
        run: npm run check

      - name: Validate Expo config
        env:
          CI: '1'
          EXPO_NO_TELEMETRY: '1'
          EXPO_OFFLINE: '1'
        run: npx expo config --type public

      - name: Commit validated changes and remove one-time patcher
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm scripts/one-time-messenger-ux-polish.mjs .github/workflows/one-time-messenger-ux-polish.yml
          git add components/PersistentBottomNavigation.tsx features/messenger/MessageReceiptsModal.tsx 'app/messenger/group/[id].tsx' 'app/messenger/room/[id].tsx'
          git commit -m "fix: refine messenger detail UX"
          git push origin HEAD:fix/minor-fixes-batch
