name: Native modules

on:
  push:
    branches:
      - main
      - agent/fix-ios-xcode-26-share
      - stabilization/native-rich-text-input

jobs:
  android-native-module:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Verify EAS archive includes native module sources
        run: |
          if git check-ignore --no-index -q modules/forward-rich-text-input/android/build.gradle; then
            echo "Android module sources are excluded by ignore rules"
            exit 1
          fi
          if git check-ignore --no-index -q modules/forward-rich-text-input/ios/ForwardRichTextInputView.swift; then
            echo "iOS module sources are excluded by ignore rules"
            exit 1
          fi

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Install dependencies
        run: npm ci

      - name: Generate Android project
        env:
          CI: "1"
          EXPO_NO_TELEMETRY: "1"
        run: npx expo prebuild --platform android --no-install

      - name: Compile Android app and native modules
        working-directory: android
        run: ./gradlew :app:compileDebugKotlin --stacktrace

  ios-native-module:
    runs-on: macos-26
    timeout-minutes: 45
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Select Xcode 26.5
        run: sudo xcode-select -s /Applications/Xcode_26.5.app/Contents/Developer

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate iOS project
        env:
          CI: "1"
          EXPO_NO_TELEMETRY: "1"
        run: npx expo prebuild --platform ios --no-install

      - name: Install CocoaPods
        working-directory: ios
        run: pod install

      - name: Compile native rich text pod
        working-directory: ios
        run: >-
          xcodebuild
          -project Pods/Pods.xcodeproj
          -target ForwardRichTextInput
          -configuration Debug
          -sdk iphonesimulator
          CODE_SIGNING_ALLOWED=NO
          build

      - name: Compile iOS app and native extensions
        working-directory: ios
        run: >-
          xcodebuild
          -workspace ForwardHockey14.xcworkspace
          -scheme ForwardHockey14
          -configuration Release
          -sdk iphonesimulator
          CODE_SIGNING_ALLOWED=NO
          build
